# Sprint 0001 — Confirmation Pass: Data-Correctness Lens

- **Reviewed commit:** `87efe3f` on `sprint-0001-contracts` (2026-08-20)
- **Reviewer:** fresh qa-verifier agent instance with adversarial data-correctness brief (read-only; independent of prior passes)
- **Prior review:** docs/reviews/sprint-0001-barrier-data-correctness.md

## Per-item verdicts

1. **F1 stale-ack remediation — PASS.** `claim_id uuid` set inside the claim UPDATE with `RETURNING`; fresh on every claim attempt ("overwriting `claim_id` **is** the revocation of the prior claim" — which also closes the same-worker-re-claim hole); all three transitions guarded `WHERE id = $event_id AND claim_id = $claim_id AND status = 'pending'`; 0-row semantics explicit ("stop modifying that row entirely — no retry of the update, no fallback write"); `claimed_by` pinned observability-only, never in a WHERE clause. All four prior interleavings re-run against the new text and **closed**: stale success-ack (guard mismatches; EvalPlanQual re-check under READ COMMITTED yields 0 rows), 0-row ack (now defined), stale failure-path clobber (guarded; no third-worker cascade), stale failed-ceiling-though-delivered (guarded; and the residual legitimate case is handled at the derivation layer — a processed receipt outranks `failed`). Fresh probes found no new defect: lease-expired-but-unreclaimed ack correctly still matches (clock-free token); `FOR UPDATE SKIP LOCKED` claim races resolve correctly; attempt_count advances only in the claim.
2. **F2 mutually exclusive derivation — PASS.** First-match precedence with a total catch-all. Probes all single-valued and specified: pending row + processed receipt → processed; failed outbox + processed receipt → processed (first-match, and correct); published + failed receipt → failed/consumer; both-failed tie → publication; processing receipt → published-or-pending with attempt detail. `processed` is absorbing (transaction A's `<> 'processed'` guard), so precedence-1 can never be lost; failed→published→processed progression across a retry is consistent — no monotonicity claimed.
3. **F3–F7 wording pins — PASS (all five).** Honest pending gloss; failed mapped with failure_stage; envelope event_id = outbox_event.id pinned in five places plus domain-model; atomicity scoped to transactional PostgreSQL side effects with the decision-record requirement for external effects; backstop-index claim correctly narrowed ("rests solely on this idempotency-key primary key"; index comment "task-per-interaction half ONLY").
4. **Receipt lifecycle probe — PASS (one minor residue, R1).** Two same-name instances: A serializes on the ON CONFLICT row lock (correct attempt counting); B serializes on the guarded UPDATE's row lock — the loser sees 0 rows (EvalPlanQual) and must ROLLBACK, so double side effects are impossible (side effects share transaction B). Crash between A and B: receipt stuck `processing`, message never acked → guaranteed redelivery, A re-attempts; derived state single-valued meanwhile. Failed-row retry vs concurrent inspection: single-statement snapshot → one derived state. attempt_count: advances only in A, committed before the work — crashed attempts counted, no double counting. CHECK unviolable by construction (`processed` absorbing; C guarded on `processing`). Cross-mechanism deadlock check: A's FK insert takes FOR KEY SHARE on the outbox row; the publisher's ack updates only non-key columns — compatible, no blocking.
5. **claim_id × receipt states — PASS.** Stale publisher after processing: guard yields 0 rows; the legitimate W0-crash re-publish flips pending→published while the derived state was and remains processed (receipt absorbing) — no text implies regression from processed; the duplicate delivery is absorbed by A's skip rule. Linkage: no uniqueness claimed on (aggregate_type, aggregate_id, aggregate_version); the §7.2 join's 1:1 assumption holds by construction in-slice; ordering explicitly delegated to partition keys — no over-claim.
6. **Membership recheck — PASS.** Lock mode precise and technically correct (FOR SHARE; FOR KEY SHARE rejected with the exactly-right non-key-update reason); two-way serialization claim matches READ COMMITTED + EvalPlanQual; placement ("statement zero of EVERY tenant-owned write") and abort semantics ("ROLLBACK immediately; no writes occur") explicit; both membership and tenant rows locked. No deadlock cycle constructible: statement zero takes only shared locks; duplicate-key writers serialize on the speculative key insert; FK checks take the weaker FOR KEY SHARE.

## Findings

**R1 (minor residue) — §6 transaction B's 0-row gloss is incomplete; a literal "then ack" can strand a `failed` receipt without a specified retry trigger.**
- Severity: minor (no double side effect, no data loss, fully inspectable; requires a two-instance duplicate delivery plus an infrastructure failure of B, and in Sprint 0001 B contains nothing but the receipt update).
- Contract/section: database-contract.md §6 step 2 comment: "0 rows updated → a concurrent attempt already processed it: ROLLBACK ... then ack."
- Concrete interleaving: instances X and Y share `consumer_name` (rebalance duplicate). Both complete A. Y's B fails on an infrastructure error before touching the receipt row → Y's C commits `status='failed'`. X's B then hits the guard (`status='processing'`) → 0 rows — but the cause is *failed*, not "already processed." Following the comment literally, X rolls back and acks; if Y's delivery is also not retried, the event rests at derived state `failed` with no specified mechanism re-driving step 1 (the "next delivery re-attempts" healing path is stated only for the `processing`-stuck crash case; retry policy for `failed` receipts is otherwise unowned).
- Owning node: D (one sentence; retry-policy ownership pointer to Node E).
- Recommended resolution: on 0 rows in B, re-read the receipt; ack only if `status='processed'`; if `status='failed'` (or `processing` under a live concurrent attempt), do not treat as processed — follow the worker's failed-delivery/retry policy (Node E). The ack decision then matches the actual receipt state instead of assuming the processed cause.

No other findings. No unresolved major items.

## Verdict

**FREEZE-READY at 87efe3f** — all prior findings verified resolved and the new mechanisms are sound under adversarial interleavings and PostgreSQL READ COMMITTED semantics; one minor one-sentence residue (R1) may ride along or be deferred without blocking.
